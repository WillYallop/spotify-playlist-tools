import siteFunction from './modules/site-function.js'

const state = () => ({
  
});

const mutations = {
  
};

const actions = {
  
};

const modules = {
  siteFunction,
  
};

export default {
  state,
  mutations,
  actions,
  modules,
}